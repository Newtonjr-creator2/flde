name: real_buzzing_identifier
description: FLDE — a mobile-first Flutter/Dart IDE foundation for Android.
publish_to: 'none'
version: 0.2.0+2

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  path_provider: ^2.1.4
  path: ^1.9.0
  file_picker: ^12.0.0
  archive: ^3.6.1
  flutter_highlight: ^0.7.0
  highlight: ^0.7.0
  crypto: ^3.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
